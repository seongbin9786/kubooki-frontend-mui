import React from 'react';

const styles = theme => ({

});

function NewsPage({ classes }) {
  // 1.

  // 2.

  // 3. 

  // 4. 
  return (
    <div>

    </div>
  );
};

export default withStyles(styles)(NewsPage);